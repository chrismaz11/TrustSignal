name: Security / zizmor Workflow Audit

on:
  pull_request:
    paths:
      - ".github/workflows/**"
  push:
    paths:
      - ".github/workflows/**"

permissions: {}

jobs:
  zizmor:
    name: zizmor advisory audit
    runs-on: ubuntu-latest
    permissions:
      contents: read
      actions: read
    steps:
      - name: Checkout
        uses: actions/checkout@1af3b93b6815bc44a9784bd300feb67ff0d1eeb3 # v4.1.7
        with:
          persist-credentials: false

      - name: Run zizmor in advisory mode
        continue-on-error: true
        uses: zizmorcore/zizmor-action@e639db99335bc9038abc0e066dfcd72e23d26fb4 # v0.3.0
        with:
          advanced-security: false
          annotations: true

      - name: Summarize zizmor mode
        if: always()
        run: |
          {
            echo "## zizmor workflow audit"
            echo ""
            echo "- Mode: advisory"
            echo "- Trigger: only when files under .github/workflows/** change"
            echo "- Findings: review annotations and job logs; the workflow does not fail the check solely for findings at this stage"
          } >> "$GITHUB_STEP_SUMMARY"
